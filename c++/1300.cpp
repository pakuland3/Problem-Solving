#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    ll n,k;
    vector<ll> p;
    cin >> n >> k;
    for(int i=1;i<=n+1;i++){
        p.push_back(i*i);
    }
    ll s=1,e=n*n;
    
    return 0;
}