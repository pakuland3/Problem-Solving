#include <bits/stdc++.h>
using namespace std;

int n,k;
int dp[100001];

void solve(){
    queue<int> q;
    q.push(n);
    while(!q.empty()){
        int now=q.front();
        
    }
}

int main(){
    cin >> n >> k;
    if(k<=n) cout << n-k;
    else solve();
}