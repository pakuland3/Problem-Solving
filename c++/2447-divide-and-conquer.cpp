#include <bits/stdc++.h>
using namespace std;

void st(int n, bool b){
    if(n==1){
        if(b) cout << '*';
        else cout << ' ';
        return;
    }
    for(int i=0;i<n;i++) cout << 1;
}

int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    return 0;
}